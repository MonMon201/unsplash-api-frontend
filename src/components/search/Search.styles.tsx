import styled from 'styled-components';
import { StyledSection } from '../../common/Section';

export const StyledSearch = styled(StyledSection)`
    width: 100%;
    flex-flow: column;
`;

export const StyledSearchBarSection = styled(StyledSection)`
    width: 60%;
`;

export const StyledGridSection = styled(StyledSection)`
    width: 80%;
`;
